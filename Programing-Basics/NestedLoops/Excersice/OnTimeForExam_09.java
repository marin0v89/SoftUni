import java.util.Scanner;

public class OnTimeForExam_09 {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        int examHour = Integer.parseInt(scan.nextLine());
        int examMinute = Integer.parseInt(scan.nextLine());
        int arivalHour = Integer.parseInt(scan.nextLine());
        int arivalMinute = Integer.parseInt(scan.nextLine());
    }
}
